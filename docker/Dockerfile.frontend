# MIND BREEZE 2.0 — Frontend Dockerfile (미리 빌드된 dist 서빙)
FROM nginx:alpine
COPY frontend/dist /usr/share/nginx/html
COPY docker/nginx/nginx-spa.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
